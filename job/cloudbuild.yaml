steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "build",
        "-t", "us-central1-docker.pkg.dev/$PROJECT_ID/hanflow/reading-job:dev",
        "-f", "job/Dockerfile",
        "."
      ]

images:
  - "us-central1-docker.pkg.dev/$PROJECT_ID/hanflow/reading-job:dev"